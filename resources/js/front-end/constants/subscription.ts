export enum ESubscription {
    USER = "user",
    USER_ID = "user_id",
    TOTAL = "total",
    STATUS = "status",
    COURSES = "courses",
    PERIOD = "period"

}

export enum EStatus {
    PAID = "paid",
    UNPAID = "unpaid",
    CANCELLED = "cancelled",
}

export enum EPeriods {
    MORNING = "morning",
    AFTERNOON = "afternoon",
    NIGHT = "night",
}